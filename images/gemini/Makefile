# Gemini CLI guest image. Everything lives in ../agent.mk; this file is just the
# per-agent data. See ../agent.mk for the targets and the knobs.

AGENT := gemini
CLI   := gemini

include ../agent.mk
